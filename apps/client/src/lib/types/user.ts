export type UserType = {
  id: number;
  email: string;
  access_token: string;
  refresh_token: string;
};

export type AuthUserType = {
  id: number;
  email: string;
  createdAt: string;
  updatedAt: string;
};
